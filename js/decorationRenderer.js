// ===================================================
// DECORATION RENDERER
// ---------------------------------------------------
// Responsible for drawing world decorations.
// ===================================================

const DecorationRenderer = {

    draw(ctx, tile, x, y) {

        if (!tile.decoration) {
            return;
        }

        // Decorations will be rendered here
        // once sprite assets are added.

    }

};